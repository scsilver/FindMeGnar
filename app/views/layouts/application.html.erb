<!DOCTYPE html>
<html>
<head>
  <title>FindMeNot</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
  <script src="//ajax.googleapis.com/ajax/libs/jqueryui/1.11.2/jquery-ui.min.js"></script>

</head>
<body>

  <div class="wrap">
  
  <!-- Fixed navbar -->
  <div class="navbar navbar-default navbar-fixed-top">
    <div class="container2">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="/">Find Me Gnar</a>
      </div>
      <div class="collapse navbar-collapse">
        <ul class="nav navbar-nav">
          <li ><a href="/resorts/">Resorts</a></li>
          <li><%= link_to 'Rides', rides_path %> </li>
          <li><a href="/">Buddies</a></li>
          <li class="dropdown">
            <a href="/" class="dropdown-toggle" data-toggle="dropdown">Resorts <b class="caret"></b></a>
            <ul class="dropdown-menu">
              <%- Resort.all.reverse.each do |r| %>
                        <li><a href="/resorts/<%=r.id%>"><%=r.name %></a></li>
              <% end%>
            
            </ul>
          </li>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
      <%= yield %></div>
</body>

      <div class="navbar navbar-default navbar-fixed-bottom">
                <div class="ticker">
                <marquee behavior="scroll" direction="left" scrollamount="2" height="16px" width=100%>
                    
                    <p > 
                        <%- Resort.all.each do |r| %>
                            <%a="| " +r.symbol + ": "  %>
                            <% if r.temp == nil  %> 
                            <%a=a + "Nil"  %>
                            <%else%>
                            <%a=a + r.temp + " F "%>
                            <%end%> 
                      <%=link_to a, resort_path(r)%>

                        <% end%>
                    </p>
                </marquee>
                </div>

            </div>
        
</html>
